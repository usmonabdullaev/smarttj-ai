services:
  ai:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: smarttj-ai
    restart: unless-stopped

    env_file:
      - .env

    networks:
      - smarttj-network

networks:
  smarttj-network:
    external: true
